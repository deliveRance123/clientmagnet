{
  "name": "client-magnet-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start",
    "dev": "npm --prefix frontend run dev"
  }
}
